<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng nhập</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

    <link rel="stylesheet" href="~/css/style-components/base.css">
    <link rel="stylesheet" href="~/css/store/login.css">
</head>

<body>

    <div class="container bg-white rounded-4">

        <form method="post" action="Login">
            <h2 class="p-4 text-center">ĐĂNG NHẬP</h2>

            <div class="row g-3">
                <div class="col-12 form-floating">
                    <input class="form-control" type="text" id="login_name" name="username" placeholder="Tên đăng nhập" required>
                    <label for="login_name" class="form-label"> Tên đăng nhập</label>
                </div>

                <div class="col-12 form-floating">
                    <input class="form-control" type="password" id="pass" name="password" placeholder="Mật khẩu" required>
                    <label for="pass" class="form-label"> Mật khẩu</label>
                </div>

                <div class="col-12">
                    <a asp-controller="Account" asp-action="ForgetPassword" class="float-end" style="color: var(--light-text);">Quên mật khẩu?</a>
                </div>

                <div class="col-12">
                    <button class="btn btn-confirm w-100" type="submit">Đăng nhập</button>
                </div>
                <p class="text-danger">@ViewBag.error</p>
                <div class="col-12 text-center">
                    <span style="color: var(--light-text);">Không có tài khoản?</span>
                    <a asp-controller="Account" asp-action="SignUp" style="color: var(--orange);"><b>Đăng kí ở đây</b></a>
                </div>
            </div>
        </form>
    </div>

</body>
</html>